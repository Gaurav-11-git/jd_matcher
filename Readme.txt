Unzip the file.
Rename the folder to jd_matcher.
Open Pycharm and then open this folder as a project.
Open terminal and execute : pip install -r requirements.txt
Now in first terminal run: python -m backend.app
Open second terminal run, cd frontend and then run: streamlit run .\streamlit_app.py

